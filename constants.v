parameter PLAYER_WIDTH = 32;  // Player width
parameter PLAYER_HEIGHT = 32; // Player height
parameter PLAYER_SPEED = 12500000 / 2;  // Player speed

parameter H_SYNC_CYCLES = H_DISPLAY + H_FRONT + H_PULSE + H_BACK; // Horizontal synchronization cycles
parameter V_SYNC_CYCLES = V_DISPLAY + V_FRONT + V_PULSE + V_BACK; // Vertical synchronization cycles


parameter H_DISPLAY = 640;
parameter V_DISPLAY = 480;
parameter H_FRONT = 16;                 
parameter H_PULSE = 96;
parameter H_BACK = 48;
parameter V_FRONT = 10;
parameter V_PULSE = 2;
parameter V_BACK = 33;


parameter RECT_WIDTH = 17;   // Rectangle width
parameter RECT_HEIGHT = 2;   // Rectangle height

parameter PLAYER_WIDTH = 32;  // Player width
parameter PLAYER_HEIGHT = 32; // Player height
parameter PLAYER_SPEED = 12500000 / 2;  // Player speed

parameter CAR_WIDTH = 32;      // Car width
parameter CAR_HEIGHT = 32;     // Car height
parameter CAR_SPEED = 12500000 / 2;     // Car speed

parameter CAR2_WIDTH = 40;     // Car 2 width
parameter CAR2_HEIGHT = 32;    // Car 2 height
parameter CAR2_SPEED = 12500000 / 2;    // Car 2 speed

parameter CAR3_WIDTH = 40;     // Car 3 width
parameter CAR3_HEIGHT = 32;    // Car 3 height
parameter CAR3_SPEED = 12500000 / 2;    // Car 3 speed

parameter CAR4_WIDTH = 50;     // Car 4 width
parameter CAR4_HEIGHT = 32;    // Car 4 height
parameter CAR4_SPEED = 12500000 / 2;    // Car 4 speed